export interface ActiveMoleConfig {
  id: number; // Index of the active mole
  typeId: number; // Type of mole
  // points: number; // Number of points per click
  visibility: number; // How long it stays visible for
}